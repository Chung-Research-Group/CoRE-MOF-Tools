# Metadata reference

For the GPU handoff, authority comes only from the exact Git and restricted-data
bindings in `TRANSFER_MANIFEST.json`, nested SHA-256 ledgers, and their
receipts. Public status strings cannot promote an exploratory snapshot.
Preserve `official_split=false` and all false target-completion/publication
flags.
